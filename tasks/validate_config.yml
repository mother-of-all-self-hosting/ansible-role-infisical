# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: (Deprecation) Catch and report renamed settings
  ansible.builtin.fail:
    msg: >-
      Your configuration contains a variable, which now has a different name.
      Please change your configuration to rename the variable (`{{ item.old }}` -> `{{ item.new }}`).
  when: "lookup('ansible.builtin.varnames', ('^' + item.old + '$'), wantlist=True) | length > 0"
  with_items:
    - { "old": "infisical_http_header_content_permission_policy", "new": "infisical_http_header_permissions_policy" }

- name: Fail if required Infisical settings not defined
  ansible.builtin.fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`).
  when: "lookup('vars', item, default='') | string | length == 0"
  with_items:
    - infisical_uid
    - infisical_gid
    - infisical_hostname
    - infisical_path_prefix
    - infisical_container_network
    - infisical_database_hostname
    - infisical_database_password
    - infisical_redis_hostname
    - infisical_environment_variables_secret_key

- name: Run if Traefik is enabled
  when: infisical_container_labels_traefik_enabled | bool
  block:
    - name: Fail if Traefik settings required for Infisical are not defined
      ansible.builtin.fail:
        msg: >-
          You need to define a required configuration setting (`{{ item.name }}`).
      when: "item.when | bool and lookup('vars', item.name, default='') | string | length == 0"
      with_items:
        - { "name": "infisical_container_labels_traefik_hostname", when: true }
        - { "name": "infisical_container_labels_traefik_path_prefix", when: true }
        - { "name": "infisical_container_labels_traefik_middleware_basic_auth_users", when: "{{ infisical_container_labels_traefik_middleware_basic_auth_enabled }}" }

    - name: Fail if infisical_container_labels_traefik_path_prefix is different than /
      ansible.builtin.fail:
        msg: >-
          infisical_container_labels_traefik_path_prefix (`{{ infisical_container_labels_traefik_path_prefix }}`) must be `/`.
          Infisical does not support hosting under a subpath yet.
      when: "infisical_container_labels_traefik_path_prefix != '/'"

    # We ensure it doesn't end with a slash, because we handle both (slash and no-slash).
    # Knowing that `infisical_container_labels_traefik_path_prefix` does not end with a slash
    # ensures we know how to set these routes up without having to do "does it end with a slash" checks elsewhere.
    - name: Fail if infisical_container_labels_traefik_path_prefix ends with a slash
      ansible.builtin.fail:
        msg: >-
          infisical_container_labels_traefik_path_prefix (`{{ infisical_container_labels_traefik_path_prefix }}`) must either be `/` or not end with a slash (e.g. `/infisical`).
      when: "infisical_container_labels_traefik_path_prefix != '/' and infisical_container_labels_traefik_path_prefix[-1] == '/'"
